# URL-SHORTNER
A simple URL Shortener API built with Go (Golang). It converts long URLs into short IDs using MD5 hashing, stores them in memory, and redirects users to the original URL. The API is tested using Postman and demonstrates HTTP routing, JSON handling, and REST API .
